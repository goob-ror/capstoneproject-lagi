// AuthPage doesn't need complex model logic
// It's a simple static page showing pending approval status
class AuthPageModel {
  // No data fetching needed for this page
  // Could be extended in the future if needed
}

export default AuthPageModel;
